spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tickle
    username: tickle
    password: tickle1234
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true